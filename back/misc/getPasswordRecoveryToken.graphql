# .graphqlconfig=/home/alvaro/Projects/deliya.es/back!

query($email: String) {
    userByEmail(email: $email) {
        id
        names
        surnames
        email
        password
        isEmailContactAllowed
    }
}

